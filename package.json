{
  "name": "fran-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FRAN GAME — closing-time dream mischief on the Bugis+ floor. Yoink, haul, scoff.",
  "scripts": {
    "sync-store": "node scripts/sync-store.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "browser": "node tools/browser-check.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
